
---

## 📘 题目描述

**Trie**（发音同 _try_，又称前缀树）是一种树形数据结构，用于高效地存储和检索字符串数据集中的键。

请你实现 `Trie`类，支持以下操作：

|方法|说明|
|---|---|
|`Trie()`|初始化前缀树|
|`void insert(String word)`|向前缀树中插入字符串 `word`|
|`boolean search(String word)`|判断 `word`是否已插入（完整单词）|
|`boolean startsWith(String prefix)`|判断是否存在以 `prefix`为前缀的单词|

---

## 💡 核心思想

- Trie 是一棵 **多叉树**
    
- 每个节点表示一个字符
    
- 从根到某个节点的路径构成一个字符串
    
- 使用 `isEnd`标记是否为完整单词的结尾
    

---

## ✅ Java 实现（你的代码，已整理）

```
import java.util.*;

class Trie {

    /** Trie 节点定义 */
    private static class TrieNode {
        Map<Character, TrieNode> children;
        boolean isEnd;

        TrieNode() {
            children = new HashMap<>();
            isEnd = false;
        }
    }

    private TrieNode root;

    /** 初始化前缀树 */
    public Trie() {
        root = new TrieNode();
    }

    /** 插入字符串 */
    public void insert(String word) {
        TrieNode node = root;
        for (char c : word.toCharArray()) {
            node.children.putIfAbsent(c, new TrieNode());
            node = node.children.get(c);
        }
        node.isEnd = true;
    }

    /** 搜索完整单词 */
    public boolean search(String word) {
        TrieNode node = find(word);
        return node != null && node.isEnd;
    }

    /** 搜索前缀 */
    public boolean startsWith(String prefix) {
        return find(prefix) != null;
    }

    /** 公共查找方法 */
    private TrieNode find(String s) {
        TrieNode node = root;
        for (char c : s.toCharArray()) {
            if (!node.children.containsKey(c)) {
                return null;
            }
            node = node.children.get(c);
        }
        return node;
    }
}
```

---

## 🧪 使用示例

```
Trie trie = new Trie();
trie.insert("apple");

trie.search("apple");   // true
trie.search("app");     // false
trie.startsWith("app"); // true

trie.insert("app");
trie.search("app");     // true
```

---

## ⏱️ 复杂度分析

|操作|时间复杂度|空间复杂度|
|---|---|---|
|插入|O(L)|O(L)|
|搜索|O(L)|O(1)|
|前缀|O(L)|O(1)|

> 其中 `L`为字符串长度

---

## 📌 总结一句话

> **Trie 用空间换时间，非常适合字符串前缀匹配场景（如自动补全、拼写检查）。**

