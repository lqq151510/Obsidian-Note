
## 题目简述

- 给定 **无重复元素**​ 的整数数组 `candidates`
    
- 目标值 `target`
    
- 找出所有 **和为 target 的组合**
    
- **同一个数字可以无限次使用**
    
- 组合之间顺序无关（如 `[2,2,3]`和 `[2,3,2]`视为同一组合）
    

---

## 解题思路（回溯 + 剪枝）

1. **排序数组**，方便提前结束递归
    
2. 从 `start`索引开始遍历，避免生成重复组合
    
3. 每次选择一个数，递归继续选（可重复）
    
4. 若剩余值 `remaining == 0`，记录当前组合
    
5. 若当前数大于剩余值，直接剪枝
    

---

## ✅ Java 实现（推荐）

```
import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

public class Solution {

    public List<List<Integer>> combinationSum(int[] candidates, int target) {
        List<List<Integer>> result = new ArrayList<>();
        Arrays.sort(candidates); // 排序，便于剪枝
        backtrack(candidates, target, 0, new ArrayList<>(), result);
        return result;
    }

    private void backtrack(
            int[] candidates,
            int remaining,
            int start,
            List<Integer> path,
            List<List<Integer>> result
    ) {
        // 成功找到一组解
        if (remaining == 0) {
            result.add(new ArrayList<>(path));
            return;
        }

        // 从 start 开始，避免重复组合
        for (int i = start; i < candidates.length; i++) {
            // 剪枝：当前数已超过剩余值
            if (candidates[i] > remaining) {
                break;
            }

            path.add(candidates[i]);
            backtrack(candidates, remaining - candidates[i], i, path, result);
            path.remove(path.size() - 1); // 回溯
        }
    }

    // 测试
    public static void main(String[] args) {
        Solution solution = new Solution();

        int[] candidates = {2, 3, 6, 7};
        int target = 7;

        System.out.println(solution.combinationSum(candidates, target));
        // 输出: [[2, 2, 3], [7]]
    }
}
```

---

## 复杂度分析

|指标|说明|
|---|---|
|时间复杂度|指数级（受组合数量限制，题目保证 ≤ 150 种）|
|空间复杂度|O(target)（递归栈深度）|

---

## 常见变体（如果你需要）

- ✅ **40. 组合总和 II**（每个数只能用一次）
    
- ✅ **216. 组合总和 III**（限定个数）
    
- ✅ 非递归 / BFS 写法
    
- ✅ 返回字符串或统计数量
    

需要哪一类，我可以继续帮你整理 👍