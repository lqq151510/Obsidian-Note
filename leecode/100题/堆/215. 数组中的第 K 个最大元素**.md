
# 一、题目描述

**215. 数组中的第 K 个最大元素（中等）**

给定整数数组 `nums`和整数 `k`，请返回数组中 **第 `k`个最大的元素**。

- **不是第 k 个不同元素**
    
- 必须设计并实现 **时间复杂度为 O(n)**​ 的算法
    

### 示例

```
输入：nums = [3,2,1,5,6,4], k = 2
输出：5
```

```
输入：nums = [3,2,3,1,2,4,5,5,6], k = 4
输出：4
```

---

# 二、你的代码（已整理）

✅ 逻辑正确

❌ **在有序数组上可能超时（TLE）**

```
class Solution {

    public int findKthLargest(int[] nums, int k) {
        // 第 k 大 = 第 (n - k) 小
        int target = nums.length - k;

        int left = 0;
        int right = nums.length - 1;

        while (true) {
            int pivotIndex = partition(nums, left, right);

            if (pivotIndex == target) {
                return nums[pivotIndex];
            } else if (pivotIndex < target) {
                left = pivotIndex + 1;
            } else {
                right = pivotIndex - 1;
            }
        }
    }

    private int partition(int[] nums, int left, int right) {
        int pivot = nums[right]; // 固定选最后一个元素
        int i = left;

        for (int j = left; j < right; j++) {
            if (nums[j] < pivot) {
                swap(nums, i, j);
                i++;
            }
        }

        swap(nums, i, right);
        return i;
    }

    private void swap(int[] nums, int i, int j) {
        int temp = nums[i];
        nums[i] = nums[j];
        nums[j] = temp;
    }
}
```

---

# 三、为什么这份代码会 **TLE**？

|问题|原因|
|---|---|
|❌ 固定 pivot|始终选 `nums[right]`|
|❌ 有序数组|每次只减少一个元素|
|❌ 时间复杂度|最坏 **O(n²)**​|

👉 示例：

```
[1,2,3,4,5,6,7]
```

每次 partition 几乎没缩小范围。

---

# 四、✅ 正确且稳定的写法（推荐）

### ✅ 加入 **随机化 Pivot**（AC 100%）

```
import java.util.Random;

class Solution {
    private static final Random RANDOM = new Random();

    public int findKthLargest(int[] nums, int k) {
        int target = nums.length - k;
        int left = 0, right = nums.length - 1;

        while (true) {
            int pivotIndex = left + RANDOM.nextInt(right - left + 1);
            int pos = partition(nums, left, right, pivotIndex);

            if (pos == target) {
                return nums[pos];
            } else if (pos < target) {
                left = pos + 1;
            } else {
                right = pos - 1;
            }
        }
    }

    private int partition(int[] nums, int left, int right, int pivotIndex) {
        int pivot = nums[pivotIndex];
        swap(nums, pivotIndex, right);

        int store = left;
        for (int i = left; i < right; i++) {
            if (nums[i] < pivot) {
                swap(nums, store++, i);
            }
        }
        swap(nums, store, right);
        return store;
    }

    private void swap(int[] nums, int i, int j) {
        int t = nums[i];
        nums[i] = nums[j];
        nums[j] = t;
    }
}
```

---

# 五、复杂度分析

|项目|复杂度|
|---|---|
|平均时间复杂度|**O(n)**​|
|最坏时间复杂度|O(n²)（概率极低）|
|空间复杂度|O(1)（迭代版）|

---

# 六、面试一句话总结 ⭐

> “我使用 **QuickSelect + 随机化 Pivot**，通过 partition 缩小搜索区间，平均时间复杂度 O(n)，避免了对有序数组的退化情况。”

---

如果你愿意，我可以再帮你：

- ✅ 改成 **递归版**
    
- ✅ 对比 **堆 vs QuickSelect**
    
- ✅ 总结 **Top-K 问题通用模板**
    
- ✅ 讲解 **为什么 BFPRT 能做到真正 O(n)**
    

随时告诉我 👍