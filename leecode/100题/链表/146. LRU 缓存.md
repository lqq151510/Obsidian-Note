

---

## 一、题目描述

**题目：146. LRU 缓存（LRU Cache）**​

**难度：中等**

请你设计并实现一个满足 **LRU (Least Recently Used，最近最少使用) 缓存**​ 约束的数据结构。

### 实现 `LRUCache`类：

- `LRUCache(int capacity)`：以正整数作为容量初始化 LRU 缓存
    
- `int get(int key)`：如果关键字 `key`存在于缓存中，则返回关键字的值；否则返回 `-1`
    
- `void put(int key, int value)`：
    
    - 如果关键字 `key`已存在，则变更其数据值
        
    - 如果不存在，则插入该组 `key-value`
        
    - 当缓存容量达到上限时，**逐出最久未使用的关键字**
        
    

### 要求

- `get`和 `put`必须以 **O(1)**​ 平均时间复杂度运行
    

---

## 二、设计思路

### 核心数据结构

|数据结构|作用|
|---|---|
|**哈希表（HashMap）**​|O(1) 时间查找 `key`对应的节点|
|**双向链表**​|维护节点使用顺序（最近使用 → 最久未使用）|

### 关键规则

- **最近使用**的节点放在链表 **头部**
    
- **最久未使用**的节点放在链表 **尾部**
    
- 每次 `get`/ `put`都将访问的节点移到头部
    
- 超出容量时删除尾部节点
    

---

## 三、Java 代码实现（推荐 ✅）

```
import java.util.HashMap;
import java.util.Map;

public class LRUCache {

    // 双向链表节点
    class Node {
        int key;
        int value;
        Node prev;
        Node next;

        Node(int key, int value) {
            this.key = key;
            this.value = value;
        }
    }

    private Map<Integer, Node> cache; // 哈希表：key -> 节点
    private int capacity;             // 缓存容量
    private Node head;                // 虚拟头节点（最近使用）
    private Node tail;                // 虚拟尾节点（最久未使用）

    public LRUCache(int capacity) {
        this.capacity = capacity;
        this.cache = new HashMap<>();

        // 初始化虚拟头尾节点
        head = new Node(-1, -1);
        tail = new Node(-1, -1);
        head.next = tail;
        tail.prev = head;
    }

    // 获取值，并将节点移到头部
    public int get(int key) {
        Node node = cache.get(key);
        if (node == null) {
            return -1;
        }
        moveToHead(node);
        return node.value;
    }

    // 插入或更新键值对
    public void put(int key, int value) {
        Node node = cache.get(key);

        if (node != null) {
            // 已存在：更新值并移到头部
            node.value = value;
            moveToHead(node);
        } else {
            // 不存在：创建新节点
            Node newNode = new Node(key, value);
            cache.put(key, newNode);
            addToHead(newNode);

            // 超出容量：删除尾部节点
            if (cache.size() > capacity) {
                Node removed = removeTail();
                cache.remove(removed.key);
            }
        }
    }

    // 将节点添加到头部
    private void addToHead(Node node) {
        node.prev = head;
        node.next = head.next;
        head.next.prev = node;
        head.next = node;
    }

    // 将节点移到头部
    private void moveToHead(Node node) {
        removeNode(node);
        addToHead(node);
    }

    // 从链表中移除节点
    private void removeNode(Node node) {
        node.prev.next = node.next;
        node.next.prev = node.prev;
    }

    // 移除尾部节点并返回
    private Node removeTail() {
        Node node = tail.prev;
        removeNode(node);
        return node;
    }
}
```

---

## 四、使用示例

```
LRUCache lru = new LRUCache(2);
lru.put(1, 1);   // 缓存：{1=1}
lru.put(2, 2);   // 缓存：{1=1, 2=2}
lru.get(1);      // 返回 1 → 缓存：{2=2, 1=1}
lru.put(3, 3);   // 淘汰 key=2 → 缓存：{1=1, 3=3}
lru.get(2);      // 返回 -1
lru.put(4, 4);   // 淘汰 key=1 → 缓存：{3=3, 4=4}
lru.get(1);      // 返回 -1
lru.get(3);      // 返回 3
lru.get(4);      // 返回 4
```

---

## 五、复杂度分析

|操作|时间复杂度|空间复杂度|
|---|---|---|
|`get`|O(1)|O(capacity)|
|`put`|O(1)|O(capacity)|

---

## 六、面试要点 ✅

- ✅ **必须同时使用哈希表 + 双向链表**
    
- ✅ 虚拟头尾节点可大幅简化边界判断
    
- ✅ 节点中必须同时存储 `key`和 `value`
    
- ✅ 淘汰节点时要同步清理哈希表
    
- ✅ 这是面试高频题，务必手写熟练
    

---
